module.exports = {
    timeout: 5000, // Tiempo máximo para la ejecución (ms)
    errorThresholdPercentage: 50, // Porcentaje de errores antes de abrir el circuito
    resetTimeout: 10000, // Tiempo antes de intentar reabrir el circuito (ms)
  };
  